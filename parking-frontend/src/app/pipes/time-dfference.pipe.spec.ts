import { TimeDfferencePipe } from './time-dfference.pipe';

describe('TimeDfferencePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeDfferencePipe();
    expect(pipe).toBeTruthy();
  });
});
