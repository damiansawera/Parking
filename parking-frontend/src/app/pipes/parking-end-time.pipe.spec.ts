import { ParkingEndTimePipe } from './parking-end-time.pipe';

describe('ParkingEndTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ParkingEndTimePipe();
    expect(pipe).toBeTruthy();
  });
});
